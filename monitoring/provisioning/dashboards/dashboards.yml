apiVersion: 1

providers:
  - name: 'Bedrock AI Gateway Dashboards'
    orgId: 1
    folder: 'AWS Bedrock Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
